<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sky.bloggerapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="15" />

    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission
        android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission
        android:name="android.permission.MANAGE_ACCOUNTS" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Light.NoTitleBar" android:name=".util.Constants">
        <activity
            android:name="Editor"
            android:label="@string/title_activity_main" >

            <!--
                 <intent-filter> <action android:name="android.intent.action.MAIN" 
				/> <category android:name="android.intent.category.LAUNCHER" /> </intent-filter>
            -->
        </activity>
        <activity
            android:name=".LoginActivity" >
        </activity>
        <activity
            android:name="ViewPosts" >
        </activity>
        <activity
            android:name="ViewPost" >
        </activity>
        <activity
            android:name="ViewBlogs" >
        </activity>
        <activity
            android:name=".PostListActivity"
            android:label="@string/title_activity_post_list" >
            
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.sky.bloggerapp.BlogListActivity" />
        </activity>
        <activity
            android:name=".PostDisplayActivity"
            android:label="@string/title_activity_post_display" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.sky.bloggerapp.PostListActivity" />
        </activity>
        <activity
            android:name=".CreatePostActivity">
            <!-- android:label="@string/title_activity_create_post" >-->
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN" />

                <category
                    android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BlogListActivity"
            android:label="@string/title_activity_blog_list" >
        </activity>
    </application>

</manifest>